CREATE TABLE folders (
  folderId INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  title TEXT NOT NULL,
  count INTEGER NOT NULL
);

ALTER TABLE notes
  ADD COLUMN
    folder INTEGER REFERENCES folders(folderId) NOT NULL;
